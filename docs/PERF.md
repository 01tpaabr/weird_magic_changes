# Performance baselines

Update when the hot path changes. Always record: machine, thread count, `n`, profile.
Run: `make bench` (criterion, `target/criterion/report/index.html`) and
`cargo run --release -p app --bin wmc -- show 4096 4096 7`. Thread count: `WMC_THREADS=n`.
Render benches (`crates/app/benches/render.rs`) are sized like a 2560x1440 window at a
16 px cell: 160x90 cells. Rows before 2026-09-23 "bevy" are from the rayon + Zig build and stay as the "before" of
the migration; the CPU blit rows were dropped with the blit (the GPU draws the tiles now).

| date | commit | machine | bench | n | result | notes |
|------|--------|---------|-------|---|--------|-------|
| 2026-09-23 | initial | Apple Silicon 8c | `wmc 1000000 600` release | 1e6 | 88.7 µs/step (53 ms / 600 steps), 8 threads | skeleton saxpy step, memory-bound |
| 2026-09-23 | stage | Apple Silicon 8c | `wmc 4096 4096 7` release, generate only | 16.8e6 cells | 60.7 ms (≈276 M cells/s), 8 threads | value-noise ground + hashed rocks, row-band chunks; 2048² = 31.8 ms incl. pool startup |
| 2026-09-23 | chunked | Apple Silicon 8c | `wmc show 4096 4096 7` release, generate only | 4096 chunks = 16.8e6 cells | 77 ms (≈218 M cells/s), 8 threads | first version copied chunks vec→slab: 102 ms; now generated in place via `cells_mut_at`. Remaining gap to the flat layout is the zero-fill of fresh slab slots (~100 MB sequential) |
| 2026-09-23 | chunked | Apple Silicon 8c | criterion `generate_many/32x32 chunks` | 4.2e6 cells | 18.2 ms (≈231 M cells/s) | pure generation, no slab |
| 2026-09-23 | chunked | Apple Silicon 8c | criterion `stage_checksum/32x32 chunks` | 4.2e6 cells | 5.2 ms | fnv1a per chunk in parallel, fold in coord order |
| 2026-09-23 | chunked | Apple Silicon 8c | criterion `ensure_loaded/sweep 1 chunk/frame, radius 2` | 5 chunks gen + 5 unload per frame | 224 µs/frame | the per-frame streaming cost while panning; budget at 60 fps is 16 ms |
| 2026-09-23 | window | Apple Silicon 8c | criterion `render_cells/160x90` | 14.4e3 cells | 50.6 µs (1 thr), 45.7 µs (8 thr) | phase 1, Stage -> CellFrame. Too small to gain from rows in parallel (rayon overhead ≈ work); keep row-parallel for 4K/zoomed-out views, revisit if it ever shows in a profile |
| 2026-09-23 | time | Apple Silicon 8c | criterion `render_cells/160x90` | 14.4e3 cells | 55.4 µs (1 thr), 49.1 µs (8 thr) | was 48.3 / 46.6 µs: +14% / +5% for scaling both colours of every cell by daylight (6 mul + 6 div-by-const per cell). Chosen over a pre-scaled palette table because 7 µs of a 16 ms frame is not worth a second palette type; revisit if phase 1 ever appears in a profile |
| 2026-09-23 | time | Apple Silicon 8c | `wmc run <dir> 1000 1024 1024 7` release | 256 chunks | 0 ns/tick, checksum `a06a0a89729ce5a9` on 1 and 8 threads | `World::step` has no systems yet, so the loop folds to `tick += n`; this row exists so the first system has a "before". Same checksum with `RAYON_NUM_THREADS=1` (now `WMC_THREADS=1`) was the determinism gate |
| 2026-09-23 | bevy | Apple Silicon 8c | criterion `generate_many/32x32 chunks` | 4.2e6 cells | 19.8 ms (212 M cells/s), 8 threads | rayon baseline 18.2 ms. First Bevy version collected one `Vec` per task and flattened them: 23.9 ms; `par_zip_mut` writes each chunk in place through disjoint slices: 19.8 ms (4x4 chunks: 354 µs, better than before). 1 vs 4 chunks per task is noise. **Before the `multi_threaded` feature was enabled on `bevy_tasks` for sim-core this was 94.8 ms: the pool ran serially. `sim-core` alone must ask for the feature** |
| 2026-09-23 | bevy | Apple Silicon 8c | criterion `stage_checksum/32x32 chunks` | 4.2e6 cells | 5.8 ms (721 M cells/s), 8 threads | was 5.2 ms: `par_map` in batches of 16 chunk entities, `Query::get` per chunk, fold in coord order |
| 2026-09-23 | bevy | Apple Silicon 8c | criterion `ensure_loaded/sweep 1 chunk/frame, radius 2` | 5 chunks gen + 5 unload per frame | 225 µs/frame | was 224 µs on rayon (250 µs with the copying `generate_many`): entity spawn/despawn costs the same as slab slots |
| 2026-09-23 | bevy | Apple Silicon 8c | criterion `step/16x16 chunks` | 256 chunks, no systems | 9.0 µs/tick | the empty `SimTick` schedule on the multi-threaded executor: ~9 µs fixed cost per tick (was `tick += 1`, 0 ns). Irrelevant at 8 TPS; caps `max` speed near 100k ticks/s. The number every new system moves |
| 2026-09-23 | bevy | Apple Silicon 8c | criterion `render_cells/160x90/8` | 14.4e3 cells | 31.2 µs, 8 threads | was 49.1 µs on rayon rows: 8-row bands on the task pool, closure chunk lookup |
| 2026-09-23 | bevy | Apple Silicon 8c | criterion `grid_upload/160x90` | 14.4e3 cells | 457 µs, 1 thread | phase 2 now: `CellFrame` -> two `TilemapChunkTileData` (two `Color::tint` sRGB->linear `powf` per cell). Replaces the 0.47 ms Zig blit of 3.7 Mpx; the GPU draws the pixels. Bevy then repacks 14.4k `TileData` per layer per frame. Candidate for a tint lookup table if it ever shows in a profile |
| 2026-09-24 | actors-1 | Apple Silicon 8c | criterion `generate_many/32x32 chunks` | 4.2e6 cells | 21.1 ms (199 M cells/s), 8 threads | was 19.8 ms: worldgen now also places seed rows (1% of walkable cells, `hash_cell` per cell + a row push), +6.7% |
| 2026-09-24 | actors-1 | Apple Silicon 8c | criterion `stage_checksum/32x32 chunks` | 4.2e6 cells | 6.6 ms, 8 threads | was 5.8 ms: the checksum now hashes each chunk's actor rows (12 + 88 B per row, ~35 rows per chunk at default density) after its cells, +14% |
| 2026-09-24 | actors-1 | Apple Silicon 8c | `wmc run <dir> 1000 1024 1024 7` release | 256 chunks, 9085 actors | 8.6 µs/tick (1 thr), 8.4 µs/tick (8 thr), checksum `f386452c111e4787` on both | still the empty `SimTick` (`step/16x16` bench 8.9 µs, unchanged): the "before" for the first actor system. Rows are 104 B each; this world holds ~0.9 MB of them |
| 2026-09-24 | actors-2 | Apple Silicon 8c | criterion `step/16x16 chunks` | 256 chunks, 9085 seed rows | 48.9 µs/tick, 8 threads | was 8.9 µs (empty schedule): Think/Apply/Compact as three `par_iter_mut` systems over 256 chunks, the 12-byte due-scan of every row, ~18 thinks per tick at cadence 512 |
| 2026-09-24 | actors-2 | Apple Silicon 8c | `wmc run <dir> 86400 1024 1024 7` release (4 game days) | 256 chunks | 21.3 µs/tick (1 thr), 42.1 µs/tick (8 thr); 9085 seeds -> 693 seeds + 1828 trees; checksum `c444875671f782b8` on both | at this load the multi-threaded executor's per-phase dispatch (3 systems x 256 entities) costs more than the work, so 8 threads lose to 1. Irrelevant at 8 TPS (125 ms per tick); revisit when thinks per tick grow, not before |
| 2026-09-24 | actors-2 | Apple Silicon 8c | `wmc run <dir> 3000 4096 4096 7` release | 4096 chunks, 143 177 seed rows | 396 µs/tick (1 thr), 187 µs/tick (8 thr); checksum `08d4f1887113dd8b` on both | ~280 thinks per tick; the rest is the due-scan of 143k rows (1.7 MB) per tick. 2.1x on 8 threads: memory-bound, as the cost model predicted |
| 2026-09-24 | actors-3 | Apple Silicon 8c | criterion `step/16x16 chunks` | 256 chunks, 9085 seed rows | 48.2 µs/tick, 8 threads | compiled `rules/plants.rules` instead of hand-assembled ops: "no change" vs the actors-2 baseline (p = 0.21). `wmc run 1024x1024 86400` gave the same populations (693 seeds, 1828 trees) at a new checksum `2e9830e76f6ecb31` (the rules hash moved: immediates instead of pool constants); its wall time read 28/60 µs/tick (1/8 thr) under a load average of 10, so the bench is the number of record |
| 2026-09-24 | actors-4 | Apple Silicon 8c | `wmc run <dir> 86400 1024 1024 7` release (4 game days) | 256 chunks: ~1.4k chickens + 9k seeds at start | 37.1 µs/tick (1 thr), 53.1 µs/tick (8 thr); 22 chickens, 701 seeds, 1844 trees at the end; checksum `97f93453c9d23077` on both | chickens think every 8 ticks and walk (in-chunk claims + the sequential Migrate phase). Most chickens dry out: sight 6 and no memory of water is the rule set as written. Load average 22 during the run |
| 2026-09-24 | actors-4 | Apple Silicon 8c | `wmc run <dir> 3000 4096 4096 7` release | 4096 chunks: 21 285 chickens + 143 177 seeds | 2654 µs/tick (1 thr), 713 µs/tick (8 thr); checksum `2dda4fb7147eedd0` on both | ~2.9k thinks per tick, i.e. ~0.9 µs per think all in on one thread (the cost model said 1.5-2.5 for a chicken: cheaper). 3.7x on 8 threads with the sequential Migrate inside. The first number for animals; the border-pen bench of §11 step 4 is this world's 4096 borders |
| 2026-09-24 | actors-4 | Apple Silicon 8c | criterion `step/16x16 chunks` | 256 chunks, 9k seeds + ~1.4k chickens | 50.4 µs/tick, 8 threads | +7% vs actors-3, but the bench world now holds chickens (default `animal_density`) and the load average was 22; not comparable, re-baseline on a quiet machine |
| 2026-09-24 | actors-5 | Apple Silicon 8c | `wmc run <dir> 86400 1024 1024 7` release (4 game days) | 256 chunks; ends at 85 chickens, 19 eggs, 18 foxes, 7675 seeds, 3474 trees | 280 µs/tick (1 thr), 150 µs/tick (8 thr); checksum `9cb381034e509613` on both | chickens now think every 4 ticks with up to three searches, foxes every 4 with sight 8; Resolve + Exchange added. The forest regrows to 11k actors by day 4. Load average 12 at the start |
| 2026-09-24 | actors-5 | Apple Silicon 8c | `wmc run <dir> 3000 4096 4096 7` release | 4096 chunks: 26 706 chickens, 1399 foxes, 143 229 seeds | 11.0 ms/tick (1 thr), 3.7 ms/tick (8 thr); checksum `de8f8c8dbcf287ee` on both | ~7.3k thinks per tick at ~1.5 µs each all in (the design's 1.5-2.5 µs chicken estimate). Was 2.65 / 0.71 ms at actors-4: content cost (2x chicken cadence, more searches), not engine. 3.7 ms is 3% of a 125 ms tick |
| 2026-09-24 | actors-5 | Apple Silicon 8c | criterion `step/16x16 chunks` | 256 chunks, built-in world at tick 0 | 192 µs/tick, 8 threads | was ~50 µs: the bench world is the default one, so it measures the new content (chickens at cadence 4, foxes) plus two more phases. Load average 5-13 during the run |
| 2026-09-24 | actors-5 | Apple Silicon 8c | criterion `generate_many/32x32 chunks` | 4.2e6 cells | 20.9 ms | unchanged (was 21.1): placement is now one hash per walkable cell from the rules' shares instead of one per kind. A first reading of 26.8 ms was taken at load 13 |
| 2026-09-25 | actors-5b | Apple Silicon 8c | `wmc run <dir> 43200 1024 1024 12` release (2 game days) | 256 chunks; ends at 1897 chickens, 1541 chicks, 160 foxes, 317k grass, 1120 seeds, 482 trees | 2170 µs/tick (1 thr), 655 µs/tick (8 thr); checksum `50a158c026fedcb6` on both | the seed's new colour moved the rules hash after timing: same populations, a rerun at load 10 gave 1065 / 2709 µs and this checksum on both. Grass is one cover row per tuft (~1/3 of the land by day 2, 98% of the rows). Split by halving one kind's think rate at a time (1 thr, first 4000 ticks, 1343 µs/tick): chickens ~940 µs (~2 µs per think, the design's estimate), grass ~130, foxes ~65, the rest is the due-scan and the phases. The egg, litter and drift rules now roll their dice before their area counts: 723 -> 655 µs (8 thr), 2467 -> 2170 µs (1 thr). Grass spread capped at 8 tufts within 2 instead of 7 within 1: 769k -> 316k grass rows, 833 -> 742 µs (8 thr, load 9-12). Next target if it shows: the per-cell halo lookup in searches |
| 2026-09-25 | actors-5b | Apple Silicon 8c | `wmc run <dir> 3000 4096 4096 7` release | 4096 chunks: 35 772 chickens, 2921 foxes, 1.21 M grass, 142 487 seeds | 21.3 ms/tick (1 thr), 5.8 ms/tick (8 thr); checksum `c626bd7832f4fbc6` on both (after the seed colour change, as above) | was 11.0 / 3.7 ms at actors-5: 1.2 M grass rows (the due-scan streams ~17 MB of 12-byte rows per tick) and hens laying on day 0. Dice before counts: 29.6 -> 21.3 ms (1 thr), 7.7 -> 5.8 ms (8 thr). 5.8 ms is 5% of a 125 ms tick. Memory: ~140 MB of rows + 64 MB of `cover` arrays |
| 2026-09-25 | actors-5b | Apple Silicon 8c | criterion `step/16x16 chunks` | 256 chunks, built-in world at tick 0 (~40k grass rows) | 646 µs/tick, 8 threads | was 192 µs at actors-5: grass rows plus hens that search more (egg rule, foraging); 659 µs before the dice reorder. Load 5-8 |
| 2026-09-25 | actors-5b | Apple Silicon 8c | criterion `generate_many/32x32 chunks` | 4.2e6 cells | 23.3 ms | was 20.9: worldgen also resets the `cover` array and pushes a grass row on 1 walkable cell in 20, +11% |
| 2026-09-25 | actors-5b | Apple Silicon 8c | criterion `stage_checksum/32x32 chunks` | 4.2e6 cells | 13.2 ms | was ~6.6 ms: the `cover` array (16 KB per chunk) and ~5x more rows per chunk are hashed, 2x |
| 2026-09-25 | actors-6b | Apple Silicon 8c | criterion `step/16x16 chunks`, A/B against actors-6a | 256 chunks, built-in world | 6a: 740, 742 µs; 6b: 725, 774, 893 µs (p 0.06-0.76), 8 threads | no significant change from `Intent` growing (`signal`, `amount`, `with`) and the transfer pass in Exchange. Load average 9-13, so absolute numbers sit above the 646 µs of actors-5b; the A/B is the number of record |
| 2026-09-25 | actors-6c | Apple Silicon 8c | criterion `step/16x16 chunks`, interleaved A/B against actors-6b | 256 chunks, built-in world (no scent) | 6b: 704, 713 µs; 6c: 735, 730 µs, 8 threads | +3%: the Simulate phase now runs `scent_decay` (a due check per chunk, a zero check of 8 KB per due chunk). As `par_iter_mut` it read 732 / 775 µs: dispatching a parallel pass for 1/16 of the chunks costs more than the work, so it runs on one thread. Load 6-14 |
| 2026-09-25 | actors-6c | Apple Silicon 8c | criterion `stage_checksum/32x32 chunks`, same A/B | 4.2e6 cells | 6b: 15.9, 13.2 ms; 6c: 15.1, 15.1 ms | the scent channels (8 KB per chunk) are hashed with the cells, within noise to +14% |
| 2026-09-25 | actors-6c | Apple Silicon 8c | criterion `render_cells/160x90/8`, same A/B | 14.4e3 cells | 6b: 41.0, 37.0 µs; 6c: 43.4, 40.8 µs | +6-10%: two scent reads and a tint per cell. A first version read the scent with `[[u8; 4096]; 2]::map` and copied 8 KB per cell: 805 µs (+1600%), caught by this bench |
| 2026-09-25 | actors-6d | Apple Silicon 8c | `wmc run <dir> 43200 1024 1024 12` release (2 game days) | 256 chunks; ends at 1895 chickens, 1553 chicks, 160 foxes, 8723 flowers, 52 hives, 1111 bees, 317k grass | 3239 µs/tick (1 thr), 1069 µs/tick (8 thr); checksum `19cc0641245304b9` on both | bees, flowers and hives add ~0.9-1.1 ms on one thread (actors-5b: 2170 / 655 µs): ~690 thinks a tick at ~1.4 µs. A first bee file with 4559 bees at 88 a hive and a 10-cell flower search on every think read 4778 / 1534 µs; smaller colonies and a bee that flies to a flower it saw without searching again (and searches only every 4th think while wandering) brought it here. Load 5-9 |
| 2026-09-25 | actors-6d | Apple Silicon 8c | `wmc run <dir> 3000 4096 4096 7` release | 4096 chunks: 35 772 chickens, 70 282 flowers, 748 hives, 1769 bees, 1.21 M grass | 25.9 ms/tick (1 thr), 8.0 ms/tick (8 thr); checksum `860ee8b4b3c916cf` on both | was 21.3 / 5.8 ms at actors-5b: flowers (1 cell in 250) and the first bees. 8 ms is 6% of a 125 ms tick |
| 2026-09-25 | actors-6d | Apple Silicon 8c | criterion `step/16x16 chunks`, interleaved A/B against actors-6c | 256 chunks, built-in world at tick 0 | 6c: 806, 817, 1050, 1247 µs; 6d: 905, 1163, 1262, 1393 µs, 8 threads | +12-20% per pair: the world now holds flowers and hives, and the first bees hatch. Background load drifted during the runs (both sides rose 50%); only the pairwise ratios are meaningful |
| 2026-09-25 | actors-7b | Apple Silicon 8c | criterion `step/16x16 chunks`, interleaved A/B against actors-7a | 256 chunks, built-in world | 8 thr: 7a 1296, 1326, 1334 µs / 7b 1350, 1070, 1323 µs; `WMC_THREADS=1`: 7a 2325, 2481, 2307 µs / 7b 2277, 2077, 2341 µs | the VM loop split into `run::<TRACE>` (op dispatch in a labelled block, a trace push when `TRACE`) and `think_one::<TRACE>`: no cost measurable, as intended. Load 7-13 |
